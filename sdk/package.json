{
  "name": "@element/sdk",
  "version": "0.1.0",
  "private": true,
  "description": "UI element inspector SDK for Element bridge",
  "scripts": {
    "build": "rollup -c",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@rollup/plugin-typescript": "^12.1.2",
    "rollup": "^4.30.1",
    "@rollup/plugin-terser": "^0.4.4",
    "tslib": "^2.8.1",
    "typescript": "^5.7.3",
    "vitest": "^3.0.4",
    "jsdom": "^25.0.1"
  }
}
